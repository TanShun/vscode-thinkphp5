<?php
namespace think;

$appRoot = 'E:/docker/crm/crm';
$entry   = $appRoot . DIRECTORY_SEPARATOR . 'thinkphp' . DIRECTORY_SEPARATOR . 'base.php';

$argv[0] = $_SERVER['argv'][0] = $appRoot . '/think';
var_dump($argv);
var_dump(realpath($_SERVER['argv'][0]));
unset($argv[1]);
unset($_SERVER['argv'][1]);
require $entry;
Container::get('app')->path($appRoot . '/application')->initialize();
Console::init();
